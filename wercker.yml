box: node

build:
  steps:
    - script:
      name: install yarn
      code: |
        npm install -g yarn
    - script:
        name: install dependencies
        code: |
           sudo apt-get install libasound2
          yarn --ignore-engines
test:
    - script:
      name: test
      code: |
        yarn test