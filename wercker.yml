box: node

build:
  steps:
    - script:
      name: install yarn
      code: |
        npm install -g yarn
    - script:
        name: install dependencies
        code: |
          apt-get install libsoundio-dev
          yarn --ignore-engines
test:
    - script:
      name: test
      code: |
        yarn test